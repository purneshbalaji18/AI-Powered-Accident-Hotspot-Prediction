numpy>=1.24.0
pandas>=1.5.0
scikit-learn>=1.2.0
# lightgbm>=3.3.0      # Optional: install for production (pip install lightgbm)
# hdbscan>=0.8.29      # Optional: install for HDBSCAN clustering (pip install hdbscan)
# shap>=0.41.0         # Optional: install for true SHAP values (pip install shap)
# folium>=0.14.0       # Optional: for Python-side map rendering
# streamlit>=1.20.0    # Optional: for Streamlit dashboard version
# tweepy>=4.14.0       # Optional: for Twitter API integration
# beautifulsoup4>=4.11 # Optional: for web scraping
# kafka-python>=2.0.2  # Optional: for real-time streaming
# flask>=2.2.0         # Optional: for Flask API server
